// Maintenance Configuration
// Set these values to enable/disable maintenance features

export const maintenanceConfig = {
  // Set to true to show maintenance page instead of homepage
  // When enabled, users will see a maintenance message
  enabled: false, // Change to true to enable maintenance mode

  // Set to true to show a 10-second popup when website loads
  // This popup will appear on all pages
  showPopup: true, // Change to false to disable the popup
}

